{
  "name": "neon-fable",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "bench": "PERF_BENCH=1 vitest run src/iso/crowdBench.test.ts src/iso/art/bakeBench.test.ts",
    "audit:narrative": "NARRATIVE_AUDIT=verbose vitest run src/narrative/audit",
    "smoke": "npm run build && node scripts/smoke.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "happy-dom": "^20.11.1",
    "typescript": "^5.5.4",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
